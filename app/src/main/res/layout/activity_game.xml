<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="16dp">

        <ImageView
            android:id="@+id/game_rooms"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:src="@drawable/drugelis"
            android:layout_marginBottom="16dp"
            android:contentDescription="Game 1" />

        <ImageView
            android:id="@+id/game_letters"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:src="@drawable/drugelis"
            android:layout_marginBottom="16dp"
            android:contentDescription="Game 2" />

        <ImageView
            android:id="@+id/game_numbers"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:src="@drawable/drugelis"
            android:layout_marginBottom="16dp"
            android:contentDescription="Game 3" />

    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>